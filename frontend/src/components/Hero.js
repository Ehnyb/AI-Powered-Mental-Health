import React from 'react';
import '../styles/Hero.css';  // Adjusted to point to the correct styles folder

const Hero = () => (
  <section className="hero">
    <h1>Start your adventure</h1>
    <button className="cta-button">Sign Up Now</button>
  </section>
);

export default Hero;